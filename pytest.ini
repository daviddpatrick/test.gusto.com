[pytest]
addopts = -ra --alluredir=allure-results -p no:rerunfailures
markers =
    ui: browser-driven UI tests
    api: direct API tests
    shared_auth: tests that reuse browser authentication for API calls
